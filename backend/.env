# ── MongoDB ──
MONGO_URI=mongodb://localhost:27017/sansyar
# یا MongoDB Atlas:
# MONGO_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/sanschi

# ── JWT ──
JWT_SECRET=mahYar_2026_super_secret_!!92
JWT_EXPIRES_IN=7d

# ── Server ──
PORT=5000
NODE_ENV=development

# ── آدرس‌ها ──
# آدرس خود بک‌اند (زرین‌پال callback را به این آدرس می‌فرستد)
BASE_URL=http://localhost:5000
# آدرس فرانت‌اند (ریدایرکت نهایی بعد از پرداخت). برای تست لوکال localhost،
# برای انتشار آدرس واقعی سایت را بگذار.
FRONTEND_URL=http://localhost:5000

# ── ایمیل (Resend) ──
RESEND_API_KEY=re_N9vbCeiL_PVTC18E5KHq5LvyejJ6TSEPn
RESEND_FROM=سانس‌چی <onboarding@resend.dev>

# ════════════════════════════════════════
# ── درگاه پرداخت زرین‌پال ──
# ════════════════════════════════════════
# مرچنت کد ۳۶ کاراکتری که از پنل زرین‌پال می‌گیری (فعلاً نمونه است)
ZARINPAL_MERCHANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# برای تست = true (درگاه sandbox)؛ در production = false
ZARINPAL_SANDBOX=true
# واحد مبلغ ارسالی به درگاه: rial یا toman (قیمت سایت تومان است → rial)
ZARINPAL_CURRENCY=rial
